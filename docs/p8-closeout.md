# P8 closeout

P8 is closed as a small, local-first diagnose-now milestone.

## Completed

- Added `doctor-link diagnose-now`.
- Writes `summary.md` for a target library.
- Reports file count.
- Reports extension counts.
- Excludes the output directory from scans so repeated runs do not change file counts.
- Supports `--json` for machine-readable summary path output.
- Supports `--output` for custom report directories.
- Documents default