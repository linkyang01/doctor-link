# P5.10 CI Validation Record

Phase: P5.10 local validation hardening before P6 authorization
Status: cloud validation passed; local validation still pending

## Purpose

This document records the remote CI and manual cloud validation evidence available before the optional local or target-environment validation run.

It does not replace `docs/p5.10-local-validation.md`. Local validation is still useful for proving the user's Mac or target delivery environment. The cloud validation result confirms that the current `main` branch builds, installs, tests, and runs the validation scripts successfully in GitHub Actions.

## Remote CI evidence

The following P5.10-related pull requests passed CI before merge:

- PR #75: `P5.10: stabilize local validation script`
- PR #76: `P5.10: align README status before local validation`
- PR #77: `P5.10: add remote closeout docs`
- PR #78: `P5.10: add manual cloud validation workflow`

The CI workflow covers:

- Python 3.10, 3.11, and 3.12 matrix validation;
- `ruff check doctor_link tests scripts`;
- `pytest` with coverage output;
- CLI smoke tests;
- end-to-end validation with `scripts/e2e_validate.sh`;
- wheel and source distribution build;
- installed wheel smoke validation;
- installed wheel execution of `scripts/validate_doctor_link.sh`.

## Manual cloud validation result

The manual cloud validation workflow was run after PR #78 was merged into `main`.

Result recorded from GitHub Actions UI:

```text
Workflow: P5.10 Manual Validation #1
Branch: main
Commit: 8abeb8b
Runner: GitHub Actions ubuntu-latest
Job: Manual P5.10 validation
Result: success
Duration: 30 seconds
```

This manual validation runs:

```bash
ruff check doctor_link tests scripts
pytest -q --cov=doctor_link --cov-report=term-missing --cov-report=xml
python -m build
bash scripts/validate_doctor_link.sh ManualCloudValidation
bash scripts/e2e_validate.sh "$(pwd)"
```

## Cloud validation conclusion

Doctor link `main` has passed cloud validation in GitHub Actions.

The repository is confirmed in cloud validation to:

- pass static checks;
- pass tests with coverage;
- build wheel and source distribution packages;
- run the usability validation script successfully;
- run the E2E validation script successfully;
- execute the CLI entrypoint successfully in the validation flow.

## Local validation remains separate

Local validation is still recommended if the project must be proven on a specific Mac, workstation, or target delivery environment.

Required local record fields:

```text
Date:
Machine / OS:
Python version:
Commit SHA:
Branch:
Validation result:
Notes:
```

## Boundaries

This record does not authorize:

- P6 implementation;
- GitHub Release creation;
- release tag creation;
- PyPI publishing;
- repository permission changes;
- paid cloud services;
- external account systems;
- new product features.
