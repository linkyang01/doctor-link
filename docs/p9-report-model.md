# P9 report model

P9 starts the structured diagnosis report work.

Current baseline:

- `doctor_link.diagnose_report.build_report(files)` returns a small structured dict.
- The report includes file count, extension counts, and recommendations.
- Existing `diagnose-now` CLI behavior is unchanged.

Next steps should use PRs only.
