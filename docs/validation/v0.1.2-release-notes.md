# Doctor link v0.1.2 Release Notes

Release date: 2026-06-29

## Summary

`v0.1.2` expands AI handoff tooling with three new profiles and adds guided-workflow CLI polish for scripted diagnosis and handoff generation.

## Highlights

- New handoff profiles: `grok`, `windsurf`, `cline`
- `doctor-link diagnose-now --tool` aligned with `wizard --tool`
- `doctor-link handoff list` and `doctor-link handoff check`
- `doctor-link handoff --json` for scripted automation
- Default guided handoff tool unified to `cursor`
- Documentation aligned to `v0.1.2` as Latest

## Install

### From GitHub Release wheel

```bash
pip install https://github.com/linkyang01/doctor-link/releases/download/v0.1.2/doctor_link-0.1.2-py3-none-any.whl
doctor-link --version
```

### From source

```bash
git clone https://github.com/linkyang01/doctor-link.git
cd doctor-link
git checkout v0.1.2
pip install -e .
doctor-link --version
```

## Quick commands

```bash
doctor-link handoff list
doctor-link wizard --folder . --summary "startup issue" --tool grok --handoff --json
doctor-link diagnose-now . --handoff --tool grok --json
doctor-link handoff check <package_dir> --tool cursor --json
doctor-link handoff <package_dir> --tool codex --json
```

## Validation

- Automated test suite (handoff list/check/generate covered)
- Python 3.10 / 3.11 / 3.12 CI matrix
- Local Mac validation recorded 2026-06-29

## Boundaries

This release does **not** include:

- hosted Web platform or cloud sync
- user accounts or telemetry
- PyPI publication (unless separately authorized)
- real signing key management

## Upgrade from v0.1.1

```bash
pip install https://github.com/linkyang01/doctor-link/releases/download/v0.1.2/doctor_link-0.1.2-py3-none-any.whl
doctor-link --version
```

Expected output: `doctor-link, version 0.1.2`